services:
  - type: web
    name: clearpath-server
    runtime: node
    buildCommand: npm install --build-from-source=sqlite3
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 18.19.1